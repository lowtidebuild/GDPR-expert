# Legal-opinion output moved

Opinion drafts and generated files now live under `GDPR_EXPERT_PRIVATE_DIR`
(default: `~/Legal-private/gdpr-expert/opinions`), outside the repo tree.

Generator scripts remain in this directory. See `.env.example` for the env var.
